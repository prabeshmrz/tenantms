spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${datasourceDevUrl}
    username: ${datasourceDevUsername}
    password: ${datasourceDevPassword}
